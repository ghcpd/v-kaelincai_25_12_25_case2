# Role & Objective

You are a senior software engineer tasked with fixing critical bugs in a logistics routing system that incorrectly applies Dijkstra's algorithm to graphs containing negative-weight edges. You must create a fixed version in a new workspace directory with proper Python environment configuration, implementing input validation, correcting algorithm defects, adding Bellman-Ford algorithm support, and ensuring all tests pass using the configured Python environment.

# Inputs & Context

## Source Project Location
- Buggy project located at: `./issue_project/`
- Contains logistics routing system with algorithm selection and validation issues
- Tests currently fail due to incorrect handling of negative-weight edges

## Target Workspace
- Create all fixed project files under: `C:\BugBash\workSpace1\oswe-mini-m23a1s255`
- Must create subdirectory: `fixed_project/` for the corrected implementation

## Project Background
This is a logistics routing system that calculates shortest paths in directed graphs. The system currently forces the use of Dijkstra's algorithm on graphs containing negative-weight edges, violating Dijkstra's fundamental precondition (non-negative weights only) and producing suboptimal paths instead of the true shortest path.

## Current Project Structure
```
./issue_project/
├── README.md                                    # Project documentation
├── KNOWN_ISSUE.md                               # Detailed bug analysis
├── requirements.txt                             # Dependencies (pytest==7.4.4)
├── pytest.ini                                   # Pytest configuration
├── data/
│   └── graph_negative_weight.json              # Graph data with negative weights
├── src/
│   └── logistics/
│       ├── __init__.py                         # Module initialization
│       ├── graph.py                            # Graph data structure and loader
│       └── routing.py                          # Routing algorithm (buggy)
└── tests/
    └── test_routing_negative_weight.py         # Test file (currently failing)
```

## Problem Manifestation

**Test Graph Structure (Directed):**
```
A → B (weight: 5)
A → C (weight: 2)
C → D (weight: 1)
D → F (weight: -3)  ← Negative weight edge
F → B (weight: 1)
A → E (weight: 1)
E → B (weight: 6)
```

**Path from A to B:**
- **Theoretically Optimal Path**: `A → C → D → F → B`, Total Cost = **1**
  - A→C: 2, C→D: 1, D→F: -3, F→B: 1
  - Total: 2 + 1 + (-3) + 1 = 1

- **Actually Returned Path**: `A → B`, Total Cost = **5** (WRONG!)

## Root Causes Identified

### 1. Algorithm Precondition Violation
- Dijkstra's algorithm does NOT support negative-weight edges
- Code doesn't check for negative weights before running Dijkstra
- When negative weights exist, Dijkstra produces incorrect results

### 2. Implementation Bug in routing.py
- **Premature Visit Marking**: Nodes are marked as visited when discovered, not when finalized from priority queue
- This prevents re-relaxation of visited nodes even when better paths are found
- Buggy code pattern:
  ```python
  visited = set([start])  # Mark start immediately
  ...
  visited.add(neighbor)   # Mark neighbor on discovery, not finalization
  ```

### 3. Missing Input Validation
- No detection of negative-weight edges before algorithm execution
- No alternative algorithm provided (e.g., Bellman-Ford) for negative-weight graphs

# Step-by-Step Instructions

## Phase 1: Analysis & Understanding

1. **Search and read all source files from the original project**:
   - Search for files matching `**/issue_project/src/logistics/*.py` pattern (expect ~3 matches)
   - Read `./issue_project/src/logistics/routing.py` - examine Dijkstra implementation
   - Read `./issue_project/src/logistics/graph.py` - examine graph data structure
   - Read `./issue_project/tests/test_routing_negative_weight.py` - understand test expectations
   - Read `./issue_project/src/logistics/__init__.py` - module initialization
   - Read `./issue_project/pytest.ini` - test configuration
   - Read `./issue_project/data/graph_negative_weight.json` - test graph data

2. **Identify specific bugs**:
   - **Bug 1 (routing.py)**: Premature visit marking in Dijkstra
     - Nodes marked as visited when discovered, not when finalized
     - Should mark nodes as visited when POPPED from priority queue
   - **Bug 2 (routing.py)**: No negative weight validation
     - Missing check: `if graph.has_negative_weights(): raise ValueError(...)`
   - **Bug 3 (graph.py)**: Missing helper methods
     - No `.has_negative_weights()` method to detect negative edges

3. **Understand test expectations**:
   - Test 1: `test_dijkstra_rejects_negative_weights` - expects `ValueError` when Dijkstra encounters negative weights
   - Test 2: `test_shortest_path_chooses_algorithm_and_finds_optimal` - expects optimal path using intelligent algorithm selection

## Phase 2: Create Fixed Project Structure

1. **Create directory structure** at `C:\BugBash\workSpace1\oswe-mini-m23a1s255\`:
   - Create `fixed_project/` directory
   - Create `fixed_project/src/logistics/` directories
   - Create `fixed_project/tests/` directory
   - Create `fixed_project/data/` directory

2. **Complete file structure**:
   ```
   fixed_project/
   ├── README.md                                    # Updated documentation
   ├── requirements.txt                             # Dependencies (pytest==7.4.4)
   ├── pytest.ini                                   # Pytest configuration
   ├── data/
   │   └── graph_negative_weight.json              # Test graph data
   ├── src/
   │   └── logistics/
   │       ├── __init__.py                         # Module initialization
   │       ├── graph.py                            # ENHANCED with helper methods
   │       └── routing.py                          # FIXED Dijkstra + NEW Bellman-Ford + shortest_path()
   └── tests/
       └── test_routing_negative_weight.py         # UPDATED tests
   ```

## Phase 3: Copy Supporting Files First

1. **Copy unchanged configuration and data files**:
   - Create and copy `requirements.txt` from original (pytest==7.4.4)
   - Create and copy `pytest.ini` from original
   - Create and copy `README.md` (will update later)
   - Create and copy `data/graph_negative_weight.json` from original
   - Create and copy `src/logistics/__init__.py` from original

## Phase 4: Implement Graph Enhancements (graph.py)

1. **Add `has_negative_weights()` method to Graph class**:
   - Purpose: Check if graph contains any negative-weight edges
   - Implementation approach:
     - Iterate through all edges in the graph
     - Return `True` if any edge has weight < 0
     - Return `False` otherwise
   - Example pattern:
     ```python
     def has_negative_weights(self):
         """Check if graph contains any negative-weight edges."""
         for source in self._adjacency:
             for target, weight in self._adjacency[source].items():
                 if weight < 0:
                     return True
         return False
     ```

2. **Preserve existing functionality**:
   - Keep all existing methods: `__init__`, `add_edge()`, `neighbors()`, `from_json()`, etc.
   - Do NOT modify graph loading or adjacency list structure

## Phase 5: Fix and Enhance Routing (routing.py)

### 5.1 Fix Dijkstra's Algorithm

1. **Add negative weight validation** at the beginning of `dijkstra_shortest_path()`:
   ```python
   def dijkstra_shortest_path(graph, start, end):
       # ADDED: Validate graph doesn't contain negative weights
       if graph.has_negative_weights():
           raise ValueError("Graph contains negative-weight edges; Dijkstra is unsafe")
       
       # ... rest of implementation
   ```

2. **Fix visit marking logic** (critical fix):
   - **BEFORE (Buggy)**:
     ```python
     visited = set([start])  # Mark start immediately
     ...
     visited.add(neighbor)   # Mark on discovery
     ```
   
   - **AFTER (Fixed)**:
     ```python
     visited = set()  # Start with empty set
     ...
     # When popping from priority queue (finalizing node):
     current_cost, current_node = heapq.heappop(priority_queue)
     
     if current_node in visited:
         continue  # Skip already finalized nodes
     
     visited.add(current_node)  # Mark as finalized HERE (when popped)
     
     # When exploring neighbors:
     if neighbor not in visited:  # Only relax if not finalized
         # ... relaxation logic
     ```

3. **Correct node finalization sequence**:
   - Mark nodes as visited only when popped from priority queue (finalized)
   - Skip nodes that have already been finalized
   - Allow re-discovery of nodes still in the queue (lower cost updates)

### 5.2 Implement Bellman-Ford Algorithm

1. **Create new function `bellman_ford_shortest_path(graph, start, end)`**:
   
   **Algorithm Overview**:
   - Time complexity: O(V × E) - slower than Dijkstra but supports negative weights
   - Relaxes all edges V-1 times (V = number of vertices)
   - Detects negative-weight cycles on Vth iteration
   
   **Implementation Requirements**:
   ```python
   def bellman_ford_shortest_path(graph, start, end):
       """
       Find shortest path using Bellman-Ford algorithm.
       Supports negative-weight edges but not negative-weight cycles.
       
       Returns: (path_list, total_cost)
       Raises: ValueError if negative-weight cycle detected
       """
       # 1. Initialize distances (all infinity except start=0)
       # 2. Get all vertices from graph
       # 3. Relax all edges V-1 times
       # 4. Check for negative cycles (Vth iteration)
       # 5. Reconstruct path from start to end
       # 6. Return (path, cost)
   ```

   **Key Implementation Details**:
   - Initialize: `distances = {node: float('inf') for all nodes}`, `distances[start] = 0`
   - Track predecessors: `predecessor = {node: None for all nodes}`
   - Relax edges: For each edge (u, v, w), if `distances[u] + w < distances[v]`, update both `distances[v]` and `predecessor[v]`
   - Negative cycle detection: After V-1 iterations, check if any edge can still be relaxed
   - Path reconstruction: Walk backwards from `end` using `predecessor` dictionary

### 5.3 Implement Auto Algorithm Selector

1. **Create `shortest_path(graph, start, end)` function**:
   
   **Purpose**: Intelligently choose between Dijkstra and Bellman-Ford based on graph properties
   
   **Implementation**:
   ```python
   def shortest_path(graph, start, end):
       """
       Find shortest path with automatic algorithm selection.
       
       Args:
           graph: Graph instance
           start: Starting node
           end: Destination node
       
       Returns: (path_list, total_cost)
       """
       # Auto-select based on graph characteristics
       if graph.has_negative_weights():
           return bellman_ford_shortest_path(graph, start, end)
       else:
           return dijkstra_shortest_path(graph, start, end)
   ```

## Phase 6: Update Tests (test_routing_negative_weight.py)

1. **Update test expectations** based on fix approach:

   **Test 1: Dijkstra Rejection Test**
   ```python
   def test_dijkstra_rejects_negative_weights():
       """Dijkstra should raise ValueError for graphs with negative weights."""
       graph = Graph.from_json("../data/graph_negative_weight.json")
       
       with pytest.raises(ValueError):
           dijkstra_shortest_path(graph, "A", "B")
   ```

   **Test 2: Auto Algorithm Selection Test**
   ```python
   def test_shortest_path_chooses_algorithm_and_finds_optimal():
       """shortest_path() should auto-choose Bellman-Ford and find optimal path."""
       graph = Graph.from_json("../data/graph_negative_weight.json")
       
       path, cost = shortest_path(graph, "A", "B")
       
       assert path == ["A", "C", "D", "F", "B"]
       assert cost == 1.0
   ```

2. **Add necessary imports**:
   - Import `dijkstra_shortest_path`, `bellman_ford_shortest_path`, `shortest_path` from routing module
   - Import `Graph` from graph module
   - Import `pytest` for exception testing

## Phase 7: Python Environment Configuration

**IMPORTANT**: Configure Python environment before running tests to ensure proper dependency management.

1. **Configure Python environment for the fixed_project**:
   - Use Python environment configuration tool for workspace: `c:\BugBash\workSpace1\oswe-mini-m23a1s255\fixed_project`
   - This ensures the correct Python interpreter is used

2. **Install project dependencies**:
   - Install pytest==7.4.4 using package installation tool
   - Command equivalent: `python -m pip install -r requirements.txt`
   - Target: `c:\BugBash\workSpace1\oswe-mini-m23a1s255\fixed_project`

## Phase 8: Test Validation

### 8.1 Initial Quick Test

1. **Run pytest in quiet mode for quick validation**:
   - Use full Python path: `C:/Users/v-kaelincai/AppData/Local/Programs/Python/Python312/python.exe`
   - Command: `python.exe -m pytest -q C:\BugBash\workSpace1\oswe-mini-m23a1s255\fixed_project -q -q`
   - Expected output: Quick pass indication

### 8.2 Verify Python Version

1. **Check Python version**:
   - Command: `C:/Users/v-kaelincai/AppData/Local/Programs/Python/Python312/python.exe --version`
   - Expected: Python 3.12.10 (or similar)

### 8.3 Run Verbose Test Suite

1. **Run pytest with verbose output**:
   - Command: `C:/Users/v-kaelincai/AppData/Local/Programs/Python/Python312/python.exe -m pytest -q C:\BugBash\workSpace1\oswe-mini-m23a1s255\fixed_project -v`
   - Expected output: 
     ```
     collected 2 items
     
     test_dijkstra_rejects_negative_weights PASSED
     test_shortest_path_chooses_algorithm_and_finds_optimal PASSED
     
     ========================== 2 passed in 0.02s ==========================
     ```

### 8.4 Get Python Environment Details

1. **Fetch Python environment information** (optional verification):
   - Get environment details for workspace path
   - Confirms Python 3.12, pytest 7.4.4 installed

## Phase 9: Update Documentation

1. **Update README.md**:
   - Change title to indicate this is the FIXED version
   - Remove "Known Issues" section or mark as RESOLVED
   - Add "Fix Summary" section explaining:
     - What bugs were fixed (Dijkstra visit marking, validation)
     - What features were added (Bellman-Ford, auto selection via `shortest_path()`)
     - Algorithm selection guidelines
   
   **Algorithm Selection Guidelines**:
   ```markdown
   ## Algorithm Selection Guide
   
   - **Dijkstra's Algorithm** (`dijkstra_shortest_path`):
     - Use for: Graphs with non-negative weights only
     - Time complexity: O((V + E) log V)
     - Behavior: Raises ValueError if negative weights detected
   
   - **Bellman-Ford Algorithm** (`bellman_ford_shortest_path`):
     - Use for: Graphs with negative weights (but no negative cycles)
     - Time complexity: O(V × E)
     - Behavior: Raises ValueError if negative-weight cycle detected
   
   - **Auto Selection** (`shortest_path`):
     - Automatically chooses optimal algorithm based on graph properties
     - Recommended for general use - safe and automatic
   ```

# Output Specification

## Directory Structure
Complete fixed project at: `C:\BugBash\workSpace1\oswe-mini-m23a1s255\fixed_project/`

## Key Deliverables

1. **Enhanced Graph Module (`src/logistics/graph.py`)**:
   - New method: `has_negative_weights()` - negative weight detection
   - All existing functionality preserved

2. **Fixed and Enhanced Routing Module (`src/logistics/routing.py`)**:
   - Fixed: `dijkstra_shortest_path()` with correct visit marking (finalize on pop) + validation
   - New: `bellman_ford_shortest_path()` for negative-weight graphs
   - New: `shortest_path()` for automatic algorithm selection (RECOMMENDED API)
   - Clear error messages for precondition violations

3. **Updated Test Suite (`tests/test_routing_negative_weight.py`)**:
   - Test 1: `test_dijkstra_rejects_negative_weights` - Dijkstra raises ValueError
   - Test 2: `test_shortest_path_chooses_algorithm_and_finds_optimal` - Auto selection finds path A→C→D→F→B with cost 1
   - All tests must PASS (2/2)

4. **Updated Documentation (`README.md`)**:
   - Mark issues as RESOLVED
   - Algorithm selection guidelines
   - Usage examples for all three functions
   - Time complexity notes

5. **Supporting Files**:
   - `pytest.ini` - test configuration
   - `requirements.txt` - dependencies (pytest==7.4.4)
   - `data/graph_negative_weight.json` - test graph data
   - `src/logistics/__init__.py` - module initialization

## Expected Test Output

**Verbose test run:**
```
============================= test session starts =============================
collected 2 items

tests/test_routing_negative_weight.py::test_dijkstra_rejects_negative_weights PASSED
tests/test_routing_negative_weight.py::test_shortest_path_chooses_algorithm_and_finds_optimal PASSED

============================== 2 passed in 0.02s ==============================
```

## Environment Verification

- **OS**: Windows
- **Python**: 3.12.10 (verified via `python --version`)
- **pytest**: 7.4.4 (installed via requirements.txt)
- **Workspace**: C:\BugBash\workSpace1\oswe-mini-m23a1s255\fixed_project

## Behavior Verification

### Dijkstra Behavior
```python
# With negative weights - should RAISE error
graph = Graph.from_json("data/graph_negative_weight.json")
dijkstra_shortest_path(graph, "A", "B")  
# Raises: ValueError("Graph contains negative-weight edges; Dijkstra is unsafe")
```

### Bellman-Ford Behavior
```python
# With negative weights - should find optimal path
graph = Graph.from_json("data/graph_negative_weight.json")
path, cost = bellman_ford_shortest_path(graph, "A", "B")
# Returns: (["A", "C", "D", "F", "B"], 1.0)

# With negative cycle - should RAISE error
graph_with_cycle = Graph(...)  # Contains negative cycle
bellman_ford_shortest_path(graph_with_cycle, "A", "B")
# Raises: ValueError("Negative-weight cycle detected")
```

### Auto Selection Behavior (RECOMMENDED)
```python
# Automatically chooses Bellman-Ford for negative weights
graph = Graph.from_json("data/graph_negative_weight.json")
path, cost = shortest_path(graph, "A", "B")
# Returns: (["A", "C", "D", "F", "B"], 1.0) via Bellman-Ford

# Automatically chooses Dijkstra for non-negative weights
graph_positive = Graph(...)
path, cost = shortest_path(graph_positive, "A", "B")
# Returns: optimal path via Dijkstra (faster)
```

# Constraints & Preferences

## Must Do Requirements
- Create fixed project in NEW directory at specified workspace path (`C:\BugBash\workSpace1\oswe-mini-m23a1s255`)
- Search for and read ALL source files before implementing fixes
- Copy configuration files FIRST (requirements.txt, pytest.ini, data files)
- Fix Dijkstra visit marking bug (mark on finalization/pop, not discovery)
- Add negative weight validation to Dijkstra (raise ValueError if detected)
- Implement Bellman-Ford algorithm with negative cycle detection
- Implement `shortest_path()` auto-selector (RECOMMENDED public API)
- Add `has_negative_weights()` helper method to Graph class
- Configure Python environment before running tests
- Install pytest==7.4.4 using package installation
- Use full Python executable path for running tests
- Update tests to verify both approaches (Dijkstra rejection, auto selection correctness)
- Ensure ALL 2 tests pass (100% pass rate)
- Update README.md with algorithm selection guidelines
- Verify Python version (3.12.10)

## Must NOT Do
- Do NOT modify the original `./issue_project/` directory
- Do NOT change graph data file (`graph_negative_weight.json`)
- Do NOT add new dependencies beyond pytest==7.4.4
- Do NOT implement algorithms that silently return incorrect results
- Do NOT skip negative cycle detection in Bellman-Ford
- Do NOT use shell multi-line commands - use single-line Python commands to avoid shell escaping issues
- Do NOT skip Python environment configuration step

## Code Quality Standards
- Clear, descriptive variable names (`distances`, `predecessor`, `visited`)
- Comprehensive docstrings for all public functions
- Explicit error messages (e.g., "Graph contains negative-weight edges; Dijkstra is unsafe")
- Comments explaining algorithm steps
- Follow PEP 8 style guidelines
- Type hints recommended

## Implementation Approach
- **Chosen Approach**: Auto-Selecting Algorithm with Explicit Options
  - Implement Dijkstra with validation (raises error on negative weights)
  - Implement Bellman-Ford separately (handles negative weights)
  - Provide `shortest_path()` as recommended public API (auto-selects)
  - Clear API: users can choose explicit algorithm or use auto-selector
  - Tests verify both Dijkstra rejection and auto-selection correctness

## Command Execution Preferences
- **Use single-line commands**: Avoid multi-line shell commands that can cause escaping issues
- **Use full Python paths**: `C:/Users/v-kaelincai/AppData/Local/Programs/Python/Python312/python.exe`
- **Configure environment first**: Always configure Python environment before running tests
- **Install dependencies**: Use package installation tool or pip install
- **Test execution**: Use `python.exe -m pytest` with full paths

# Quality Gates

## Success Criteria - All Must Be Met

### Phase 1: File Reading & Analysis
1. ✅ Search finds ~3 Python files in src/logistics
2. ✅ routing.py read and Dijkstra bug identified
3. ✅ graph.py read and missing methods identified
4. ✅ test_routing_negative_weight.py read and expectations understood
5. ✅ Additional files read (pytest.ini, __init__.py, graph data)

### Phase 2: Project Structure Creation
6. ✅ `fixed_project/` directory created at workspace path
7. ✅ All subdirectories created (src/logistics, tests, data)
8. ✅ File structure matches specification

### Phase 3: Supporting Files
9. ✅ requirements.txt copied/created (pytest==7.4.4)
10. ✅ pytest.ini copied
11. ✅ graph_negative_weight.json copied to data/
12. ✅ __init__.py copied
13. ✅ README.md created (will update later)

### Phase 4: Graph Enhancements
14. ✅ `graph.py` enhanced with `has_negative_weights()` method
15. ✅ Existing graph functionality preserved

### Phase 5: Routing Fixes & Enhancements
16. ✅ Dijkstra fixed: visit marking on finalization (when popped), not discovery
17. ✅ Dijkstra enhanced: negative weight validation added (raises ValueError)
18. ✅ Bellman-Ford implemented: correct relaxation, negative cycle detection
19. ✅ `shortest_path()` implemented: auto-selects between Dijkstra and Bellman-Ford
20. ✅ All algorithms return correct format: (path_list, total_cost)

### Phase 6: Test Updates
21. ✅ Test 1 updated: `test_dijkstra_rejects_negative_weights` - expects ValueError
22. ✅ Test 2 updated: `test_shortest_path_chooses_algorithm_and_finds_optimal` - expects path=["A","C","D","F","B"], cost=1.0
23. ✅ All necessary imports added (dijkstra_shortest_path, bellman_ford_shortest_path, shortest_path, Graph, pytest)

### Phase 7: Python Environment Configuration
24. ✅ Python environment configured for fixed_project workspace
25. ✅ pytest==7.4.4 installed using package installation tool

### Phase 8: Validation
26. ✅ Initial quick test run: `pytest -q -q -q` (quick pass check)
27. ✅ Python version verified: `python --version` (3.12.10)
28. ✅ Verbose test run: `pytest -v`
29. ✅ All 2 tests PASSED (100% pass rate)
30. ✅ Execution time reasonable (~0.02s)
31. ✅ Environment details fetched (optional verification)
32. ✅ No errors, warnings, or failures

### Phase 9: Documentation
33. ✅ README.md updated with fix summary
34. ✅ Algorithm selection guidelines added
35. ✅ Usage examples provided for all three functions

### Behavioral Verification
36. ✅ Dijkstra raises ValueError for negative-weight graphs
37. ✅ Bellman-Ford finds correct optimal path (A→C→D→F→B, cost=1)
38. ✅ `shortest_path()` auto-selects Bellman-Ford for negative-weight graphs
39. ✅ `shortest_path()` auto-selects Dijkstra for non-negative graphs

## Critical Implementation Notes

### Dijkstra Visit Marking Fix (Most Important Bug)
- **Bug Pattern**: `visited.add(neighbor)` during neighbor exploration
- **Fix Pattern**: 
  ```python
  current_cost, current_node = heapq.heappop(priority_queue)
  if current_node in visited:
      continue
  visited.add(current_node)  # Mark when popped (finalized)
  ```
- **Rationale**: Nodes should be marked as "finalized" only when their minimum distance is confirmed (when popped from priority queue), not when first discovered

### Bellman-Ford Relaxation
- **Relaxation Condition**: `if distances[u] + weight < distances[v]:`
- **Update Operations**: Update both `distances[v]` and `predecessor[v]`
- **Iteration Count**: Exactly V-1 iterations (V = number of vertices)
- **Cycle Detection**: If any edge can be relaxed on Vth iteration, negative cycle exists

### Auto Algorithm Selection (`shortest_path`)
- **Selection Logic**:
  ```python
  if graph.has_negative_weights():
      use Bellman-Ford  # Slower but handles negatives
  else:
      use Dijkstra      # Faster for non-negative graphs
  ```
- **Recommended API**: `shortest_path()` is the safest, most user-friendly option
- **Explicit Options**: Users can still call `dijkstra_shortest_path()` or `bellman_ford_shortest_path()` directly if needed

### Test Graph Expected Results
- Graph: `A→B(5), A→C(2), C→D(1), D→F(-3), F→B(1), A→E(1), E→B(6)`
- Optimal A→B: `A→C→D→F→B` with cost **1** (not A→B with cost 5)
- Path calculation: 2 + 1 + (-3) + 1 = 1

### Python Environment Configuration
- **Configure FIRST**: Before running any tests or installing packages
- **Target**: `c:\BugBash\workSpace1\oswe-mini-m23a1s255\fixed_project`
- **Install pytest**: Use package installation tool with pytest==7.4.4
- **Full Python path**: `C:/Users/v-kaelincai/AppData/Local/Programs/Python/Python312/python.exe`
- **Command format**: `python.exe -m pytest -q [path] -v`

### Single-Line Command Preference
- **Avoid multi-line shell commands**: Can cause escaping and parsing issues
- **Use Python -c for inline code**: `python -c "import sys; print(sys.version)"`
- **Use -m for modules**: `python -m pytest` instead of `pytest` directly
- **Full paths**: Use absolute paths for reliability

Execute all steps systematically. Configure Python environment before tests. Read all source files before creating fixes. Implement Dijkstra (with validation), Bellman-Ford, and shortest_path() auto-selector. Use single-line commands with full Python paths. Ensure all 2 tests pass. Update documentation with clear algorithm selection guidelines.
